apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: pipaflow

resources:
  - namespace.yaml
  - deployment.yaml
  - service.yaml
  - ingress.yaml

images:
  - name: registry.tpforge.com/pipaflow
    newTag: main
